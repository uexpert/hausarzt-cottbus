You are NOT allowed to change anything.

Your task is to ANALYZE the existing .NET backend and document its architecture.

Return ONLY a structural overview.

Required output:

1. Project structure (Controllers, Services, Repositories, Models, DTOs, etc.).
2. How Controllers call Services and how Services access data.
3. Database access technology used (EF Core, Dapper, raw SQL, etc.).
4. Main Absence-related endpoints (if they exist).
5. Dependency Injection structure (how services are registered).
6. DO NOT refactor.
7. DO NOT generate new classes.
8. DO NOT propose architecture changes.

This is a READ-ONLY analysis.

# docs-detect-structure.md

Detect structural changes in the Fullstack project and update documentation accordingly.

Tasks:
1. Detect new folders, modules, components, or backend endpoints.
2. Create new Markdown files in `/docs` if needed.
3. Update references in PROJECT_OVERVIEW.md.

Usage:
- Run after adding new modules, folders, or backend endpoints.
# Defense in depth: deny direct download of backup-config.php.
# PHP files are normally interpreted by the server, but if mod_php
# is ever disabled the file would otherwise leak its contents.
<Files "backup-config.php">
  Require all denied
</Files>

import { HyphenateDirective } from './hyphenate.directive';

describe('HyphenateDirective', () => {
  it('should create an instance', () => {
    const directive = new HyphenateDirective();
    expect(directive).toBeTruthy();
  });
});
